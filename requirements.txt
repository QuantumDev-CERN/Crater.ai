# API & serving
anthropic>=0.30.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.1.0

# PDF processing — handles text, tables, images in one pass
unstructured[pdf]>=0.14.0
unstructured-inference>=0.7.0   # table & image detection models
pypdfium2>=4.30.0
Pillow>=10.4.0

# LangChain stack
langchain>=0.2.0
langchain-core>=0.2.0
langchain-qdrant>=0.1.0
langchain-huggingface>=0.0.3
langchain-text-splitters>=0.2.0

# Vector DB
qdrant-client>=1.9.0

# SQL ORM
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# Utilities
python-dotenv>=1.0.0
pydantic-settings>=2.2.0
tenacity>=8.3.0
loguru>=0.7.0
